export function NotePreview(props) {




        switch (props.noteType){
            case 'txt':
                return <></>
                case: 'img':
                return
                case: 'video'
                return
                case 'todo'
                return
        }

        return <section className="note-preview">
            
        </section>

}
